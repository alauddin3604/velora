name: velora
recipe: laravel
config:
  database: postgres
  webroot: public
  via: nginx
  php: "8.5"
  cache: redis
services:
  node:
    type: node:24
    ssl: true
    sslExpose: true
    build:
      - npm install -g pnpm@latest-10
      - pnpm install
    command: pnpm dev
    ports:
      - 5173:5173
  database:
    creds:
      database: velora
    portforward: 5454
tooling:
  dev:
    service: node
    cmd: pnpm dev
  pnpm:
    service: node
    cmd: pnpm
excludes:
  - node_modules
  - vendor
  - .pnpm-store